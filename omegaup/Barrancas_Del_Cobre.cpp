#include <bits/stdc++.h>
#define forik(i, j, k, in) for (int i=j; i<k; i+=in)
#define forn(n, in) for (int i=0; i<n; i+=in)
using namespace std;


int main(){
    int n, k; cin>>n>>k;    
    forn(n, 1)cout<<i;
}